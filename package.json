{
  "name": "personal-knowledge-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A GitHub-native personal knowledge base for reusable agent skills and notes.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "gray-matter": "4.0.3",
    "marked": "16.3.0",
    "sanitize-html": "2.17.0"
  }
}
